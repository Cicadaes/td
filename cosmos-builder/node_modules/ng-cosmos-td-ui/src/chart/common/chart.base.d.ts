import * as echarts from 'echarts';
export declare class BaseChart {
    private chart;
    private timer;
    private resizeBindHandler;
    protected chartOption: any;
    constructor();
    readonly echarts: echarts.ECharts;
    protected chartContainer: HTMLDivElement;
    private init;
    protected inputOption(): void;
    protected onInitChart(): void;
    protected disposeChart(): void;
    protected onDestroyChart(): void;
    protected resizeEnable: boolean;
    protected resizeHandler(): void;
}
