import { Observable } from 'rxjs/Observable';
import { IThemeModuleOption, ITheme } from './theme.option';
export { ITheme } from './theme.option';
export declare const light: string;
export declare const dark: string;
export declare class ThemeService {
    private options;
    defaultTheme: string;
    private _currentTheme;
    private themeSource;
    missionTheme$: Observable<ITheme>;
    constructor(options: IThemeModuleOption | undefined);
    readonly currentTheme: string;
    use(themeName: string): void;
    private getCurrentTheme;
}
