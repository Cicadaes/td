<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.talkingdata.marketing.core.dao.campaign.EquityConfigDao" >
  <select id="findByCampaignId" resultMap="BaseResultMap" parameterType="java.lang.Integer">
    select <include refid="Base_Column_List" />
    from TD_MKT_EQUITY_CONFIG
    where `campaign_id` = #{campaignId}
  </select>

  <select id="findByName" resultMap="BaseResultMap" parameterType="map">
    select <include refid="Base_Column_List" /> from TD_MKT_EQUITY_CONFIG
    where `campaign_id` = #{campaignId} and ( `name` = #{equityConfigName}
    <if test="attachmentName != null" >
      OR `attachment_name` = #{attachmentName}
    </if>
    )
    </select>
</mapper>
